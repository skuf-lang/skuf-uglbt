@ai("Logger middleware — logs each request")
fn logger() -> fn

@ai("CORS middleware — sets Access-Control-Allow-Origin headers")
fn cors() -> fn

@ai("Rate limiter middleware")
fn rate_limit(max: int, window: int) -> fn
